# Quix Project Descriptor
# This file describes the data pipeline and configuration of resources of a Quix Project.

metadata:
  version: 2.0

# This section describes the Deployments of the data pipeline
deployments:
  - name: QuixLakeSinkEventCaller
    application: quixlakesinkeventcaller
    version: latest
    deploymentType: Service
    resources:
      cpu: 200
      memory: 500
      replicas: 2
    variables:
      - name: HIVE_COLUMNS
        value: year,month,day
      - name: CATALOG_AUTH_TOKEN
        secretKey: quix_lake_pat
      - name: COMMIT_INTERVAL
        value: 1
      - name: STREAM_TIMEOUT_SECONDS
        value: 5
      - name: STREAM_TIMEOUT_TOPIC
        value: timeout-event-topic
    blobStorage:
      bind: true
  - name: Timeout Test Producer
    application: timeout-test-producer
    version: latest
    deploymentType: Service
    resources:
      cpu: 200
      memory: 200
      replicas: 1
    desiredStatus: Running
    variables:
      - name: output
        value: timeouted-data

# This section describes the Topics of the data pipeline
topics:
  - name: timeouted-data
    configuration:
      partitions: 4
      replicationFactor: 1
      retentionInMinutes: -1
      retentionInBytes: 104857600
  - name: timeout-event-topic
